name: satellite-loop-evidence-gate
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run satellite loop evidence gate
        run: python experiments/satellite_loop_evidence_gate.py
      - name: Show result
        if: always()
        run: cat results/satellite_loop_evidence_gate.json || true
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: satellite-loop-evidence-gate-result
          path: results/satellite_loop_evidence_gate.json
