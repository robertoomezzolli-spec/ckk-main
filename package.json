{
  "name": "ckk-live-fan",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/*.test.mjs",
    "test:browser": "playwright test",
    "test:all": "npm test && python3 -m unittest discover -s test -p 'test_*.py' && npm run build"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.1",
    "three": "^0.185.1",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "netlify-cli": "^23.0.0",
    "vite": "^8.2.2"
  }
}
